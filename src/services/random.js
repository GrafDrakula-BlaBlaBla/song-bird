import { arrNameBirds } from './data';

export const randomNumber = (max = 20) => {
    return Math.floor(Math.random() * (max - 1 + 1)) + 1;
}

export const randomName = (arr, lengthNewArr, fn) => {

}
